{
  // This file is git-ignored — safe place for LOCAL dev secrets only.
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Port=5432;Database=educore;Username=postgres;Password=root"
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // LOCAL SMTP creds (git-ignored). Flip Enabled=true and fill these to actually send.
  // Gmail requires a 16-char APP PASSWORD (with 2-Step Verification on) — your normal
  // account password will be rejected. Google Account > Security > App passwords.
  // Paste the app password (no spaces) into "Password" below.
  "Email": {
    "Enabled": true,
    "Host": "smtp.gmail.com",
    "Port": 587,
    "UseSsl": true,
    "Username": "iteducore@gmail.com",
    "Password": "tldwymhidrvqudcr",
    "FromAddress": "iteducore@gmail.com",
    "FromName": "EduCore"
  }
}
