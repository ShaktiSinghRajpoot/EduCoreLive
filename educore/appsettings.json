{
  // Connection string is NOT stored here.
  // Dev: set via user-secrets ->  dotnet user-secrets set "ConnectionStrings:DefaultConnection" "..."
  // Prod: set via environment variable -> ConnectionStrings__DefaultConnection
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*"
}
