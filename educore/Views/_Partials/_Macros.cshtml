@*
  SmartSchoolWala.com brand mark (the orange "S"). Rendered as an <img> so the one
  vector is reused everywhere instead of inlining the paths into every page. Size
  it from the caller's CSS (.app-brand-logo img, .brand-box img) — not from here.
*@
<img src="~/img/brand/ssw-icon.svg" alt="SmartSchoolWala.com" class="ssw-mark" />
